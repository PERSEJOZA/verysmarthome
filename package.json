{
  "name": "smarthome",
  "version": "1.0.0",
  "description": "main environment",
  "main": "index.js",
  "scripts": {
    "ng": "ng",
    "start": "cd client && ng serve; cd server && npm start",
    "prettierRepository": "prettier --write \"**/*.{ts,yaml,html,md,scss,json}\""
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "client/**/*.ts": [
      "prettier --write",
      "npm run --prefix client lint",
      "git add"
    ],
    "nestjs-server/**/*.ts": [
      "prettier --write",
      "npm run --prefix nestjs-server lint",
      "git add"
    ],
    "*.{yaml, html, md, scss, json}": [
      "prettier --write",
      "git add"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PERSEJOZA/smarthome.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/PERSEJOZA/smarthome/issues"
  },
  "homepage": "https://github.com/PERSEJOZA/smarthome#readme",
  "dependencies": {},
  "devDependencies": {
    "husky": "^3.0.5",
    "lint-staged": "^9.2.5",
    "prettier": "1.18.2"
  }
}